import { StyleSheet } from 'react-native';

export const alingCenter = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});